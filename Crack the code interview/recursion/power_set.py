'''
--how can build all subsets of {a,b,c} from subsets of {a,b}?
--anything that's subset of ab is also subset of abc.  which sets are subsets of abc but not ab?
--mapping each subset to a binary number. ith bit could represent a boolean flag for whether
an element is in the set


be able to get n elements
'''




def power_set(a):
    
    